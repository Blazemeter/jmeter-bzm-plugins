package com.bytezone.dm3270.display;

/**
 * Mock of the original class from dm3270 required by the lib.
 * <p/>
 * No need to include fields since they are only used in original library for draw and we have
 * removed such method since we don't need to draw anything, we just return same ScreenContext every
 * time since it makes no difference. drawing.
 */
public class ScreenContext {

}
